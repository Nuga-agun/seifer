[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
seifer_core = { path = "../seifer_core" }
clap = { version = "4.6.1", features = ["cargo", "derive"] }
itertools = "0.15.0"
hex = "0.4.3"
